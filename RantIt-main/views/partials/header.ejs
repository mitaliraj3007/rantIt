<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RantIt</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap">
    <link rel="stylesheet" href="/css/style.css">  <!-- ✅ Corrected CSS path -->
</head>
<body>
    <header>
        <nav>
            <div class="logo">
                <h1>RantIt</h1>
            </div>
    
            <!-- Hamburger Menu Icon -->
            <div class="hamburger" onclick="toggleMenu()">
                &#9776;
            </div>
    
            <ul class="nav-links">
                <li><a href="/">Home</a></li>
                <% if (user) { %>
                    <li>
                        <form action="/rant" method="GET">
                            <button type="submit" class="nav-button">Start a Rant</button>
                        </form>
                    </li>
                    <li><a href="/rants">See Rants</a></li>
                    <li>
                        <form action="/logout" method="POST">
                            <button type="submit" class="nav-button logout-btn">Logout</button>
                        </form>
                    </li>
                <% } else { %>
                    <li><a href="/register">Register</a></li>
                    <li><a href="/login">Login</a></li>
                <% } %>
            </ul>
        </nav>
    </header>
    
    
    <main>
